--- oldfmt.go
+++ ARMAZEM_GRATIDAO_PRO.go
@@ -23,6 +23,8 @@
 	"unsafe"
 )
 
+const currentVersion = "1.2.17"
+
 var (
@@ -749,7 +751,7 @@
-		if versionGreater(m.Version, "1.2.11") {
+		if versionGreater(m.Version, currentVersion) {
@@ -770,7 +772,7 @@
-	header("Atualizações", "Atualizador automático do ERP Gratidão — verifica, valida, faz backup, instala e reinicia.")
+	header("Atualizações", "Atualizador automático do ARMAZEM GRATIDÃO PRO — verifica, valida, faz backup, instala e reinicia.")
@@ -795,6 +797,12 @@
 	go func() {
+		if !versionGreater(m.Version, currentVersion) {
+			if !strings.EqualFold(m.Version, currentVersion) {
+				pPostMessageW.Call(mainWnd, WM_APP_UPDATENONE, 0, 0)
+			}
+			return
+		}
 		if !strings.HasPrefix(strings.ToLower(m.PackageURL), "https://") {
@@ -804,11 +812,9 @@
 		upd := filepath.Join(root, "Updates")
-		stage := filepath.Join(upd, "stage-"+m.Version)
 		os.MkdirAll(upd, 0755)
-		os.RemoveAll(stage)
-		os.MkdirAll(stage, 0755)
-		zipPath := filepath.Join(upd, "ERP_Gratidao_"+m.Version+".zip")
+		exeNew := filepath.Join(upd, "ARMAZEM_GRATIDAO_PRO_v"+m.Version+".new.exe")
+		os.Remove(exeNew)
@@ -820,7 +826,7 @@
-		f, e := os.Create(zipPath)
+		f, e := os.Create(exeNew)
@@ -834,64 +840,15 @@
 		if !strings.EqualFold(got, strings.TrimSpace(m.SHA256)) {
+			os.Remove(exeNew)
 			setUpdateError(fmt.Errorf("SHA-256 não confere. Atualização cancelada"))
 			return
 		}
-		zr, e := zip.OpenReader(zipPath)
-		if e != nil {
-			setUpdateError(e)
-			return
-		}
-		for _, z := range zr.File {
-			name := filepath.Clean(z.Name)
-			if strings.Contains(name, "..") {
-				continue
-			}
-			dst := filepath.Join(stage, name)
-			if z.FileInfo().IsDir() {
-				os.MkdirAll(dst, 0755)
-				continue
-			}
-			os.MkdirAll(filepath.Dir(dst), 0755)
-			rc, e := z.Open()
-			if e != nil {
-				zr.Close()
-				setUpdateError(e)
-				return
-			}
-			wf, e := os.Create(dst)
-			if e != nil {
-				rc.Close()
-				zr.Close()
-				setUpdateError(e)
-				return
-			}
-			_, e = io.Copy(wf, rc)
-			wf.Close()
-			rc.Close()
-			if e != nil {
-				zr.Close()
-				setUpdateError(e)
-				return
-			}
-		}
-		zr.Close()
-		exeNew := ""
-		filepath.Walk(stage, func(path string, info os.FileInfo, e error) error {
-			if e == nil && info != nil && !info.IsDir() && strings.EqualFold(info.Name(), "ERP Gratidao.exe") {
-				exeNew = path
-			}
-			return nil
-		})
-		if exeNew == "" {
-			setUpdateError(fmt.Errorf("pacote não contém ERP Gratidao.exe"))
-			return
-		}
-		// Helper transparente: só executa depois que o ERP fechar. Faz backup e troca atômica do executável.
 		bat := filepath.Join(upd, "instalar-"+m.Version+".bat")
 		backup := filepath.Join(root, "backups")
 		os.MkdirAll(backup, 0755)
-		txt := fmt.Sprintf("@echo off\r\nsetlocal\r\ntitle ERP Gratidao - Atualizacao %s\r\n:wait\r\ntasklist /FI \"PID eq %d\" 2>NUL | find \"%d\" >NUL\r\nif not errorlevel 1 (timeout /t 1 /nobreak >NUL & goto wait)\r\nif not exist \"%s\" mkdir \"%s\"\r\nif exist \"%s\" copy /Y \"%s\" \"%s\" >NUL\r\nif exist \"%s\" copy /Y \"%s\" \"%s\" >NUL\r\ncopy /Y \"%s\" \"%s\" >NUL\r\nif errorlevel 1 goto rollback\r\nstart \"\" \"%s\"\r\nexit /b 0\r\n:rollback\r\nif exist \"%s\" copy /Y \"%s\" \"%s\" >NUL\r\nstart \"\" \"%s\"\r\n", m.Version, os.Getpid(), os.Getpid(), backup, backup, filepath.Join(root, "data", "erp.sqlite"), filepath.Join(root, "data", "erp.sqlite"), filepath.Join(backup, "erp_pre_"+m.Version+".sqlite"), filepath.Join(root, "ERP Gratidao.exe"), filepath.Join(root, "ERP Gratidao.exe"), filepath.Join(backup, "ERP_Gratidao_pre_"+m.Version+".exe"), exeNew, filepath.Join(root, "ERP Gratidao.exe"), filepath.Join(root, "ERP Gratidao.exe"), filepath.Join(backup, "ERP_Gratidao_pre_"+m.Version+".exe"), filepath.Join(backup, "ERP_Gratidao_pre_"+m.Version+".exe"), filepath.Join(root, "ERP Gratidao.exe"), filepath.Join(root, "ERP Gratidao.exe"))
+		target := filepath.Join(root, "ERP Gratidao.exe")
+		txt := fmt.Sprintf("@echo off\r\nsetlocal\r\ntitle ARMAZEM GRATIDAO PRO - Atualizacao %s\r\n:wait\r\ntasklist /FI \"PID eq %d\" 2>NUL | find \"%d\" >NUL\r\nif not errorlevel 1 (timeout /t 1 /nobreak >NUL & goto wait)\r\nif not exist \"%s\" mkdir \"%s\"\r\nif exist \"%s\" copy /Y \"%s\" \"%s\" >NUL\r\nif exist \"%s\" copy /Y \"%s\" \"%s\" >NUL\r\ncopy /Y \"%s\" \"%s\" >NUL\r\nif errorlevel 1 goto rollback\r\nstart \"\" \"%s\"\r\ndel \"%%~f0\"\r\nexit /b 0\r\n:rollback\r\nif exist \"%s\" copy /Y \"%s\" \"%s\" >NUL\r\nstart \"\" \"%s\"\r\n", m.Version, os.Getpid(), os.Getpid(), backup, backup, filepath.Join(root, "data", "erp.sqlite"), filepath.Join(root, "data", "erp.sqlite"), filepath.Join(backup, "erp_pre_"+m.Version+".sqlite"), target, target, filepath.Join(backup, "ERP_Gratidao_pre_"+m.Version+".exe"), exeNew, target, target, filepath.Join(backup, "ERP_Gratidao_pre_"+m.Version+".exe"), filepath.Join(backup, "ERP_Gratidao_pre_"+m.Version+".exe"), target, target)
@@ -910,8 +867,12 @@
 	execSQL("PRAGMA wal_checkpoint(FULL)")
+	comspec := os.Getenv("COMSPEC")
+	if comspec == "" {
+		comspec = `C:\\Windows\\System32\\cmd.exe`
+	}
 	attr := &syscall.ProcAttr{Dir: root, Env: os.Environ(), Sys: &syscall.SysProcAttr{HideWindow: false}}
-	_, _, e := syscall.StartProcess(os.Getenv("COMSPEC"), []string{"cmd.exe", "/c", bat}, attr)
+	_, _, e := syscall.StartProcess(comspec, []string{comspec, "/c", bat}, attr)
@@ -1757,9 +1718,9 @@
-	q := "SELECT id,sale_number,datetime(created_at,'localtime'),customer_name,payment_method,printf('%.2f',total),status FROM sales"
+	q := "SELECT id,sale_number,datetime(created_at,'localtime'),customer_name,payment_method,printf('%.2f',total),status FROM sales WHERE status<>'EXCLUIDA_LIMPA'"
 	if today {
-		q += " WHERE date(created_at,'localtime')=date('now','localtime')"
+		q += " AND date(created_at,'localtime')=date('now','localtime')"
@@ -2280,7 +2241,7 @@
-	brand, _, _ := pCreateWindowExW.Call(0, uintptr(unsafe.Pointer(ws("STATIC"))), uintptr(unsafe.Pointer(ws("ERP GRATIDÃO"))), WS_CHILD|WS_VISIBLE, 56, 14, 205, 27, mainWnd, 0, 0, 0)
+	brand, _, _ := pCreateWindowExW.Call(0, uintptr(unsafe.Pointer(ws("STATIC"))), uintptr(unsafe.Pointer(ws("ARMAZEM GRATIDÃO PRO"))), WS_CHILD|WS_VISIBLE, 56, 14, 205, 27, mainWnd, 0, 0, 0)
@@ -2288,7 +2249,7 @@
-	ver, _, _ := pCreateWindowExW.Call(0, uintptr(unsafe.Pointer(ws("STATIC"))), uintptr(unsafe.Pointer(ws("v1.2.11"))), WS_CHILD|WS_VISIBLE, 820, 18, 48, 20, mainWnd, 0, 0, 0)
+	ver, _, _ := pCreateWindowExW.Call(0, uintptr(unsafe.Pointer(ws("STATIC"))), uintptr(unsafe.Pointer(ws("v"+currentVersion))), WS_CHILD|WS_VISIBLE, 820, 18, 48, 20, mainWnd, 0, 0, 0)
@@ -2336,7 +2297,7 @@
-	mainWnd, _, _ = pCreateWindowExW.Call(0, uintptr(unsafe.Pointer(cls)), uintptr(unsafe.Pointer(ws("ERP Gratidão - Nativo v1.2.11 • Cabeçalho 12.1.5 + Atualizador Automático"))), WS_OVERLAPPEDWINDOW|WS_VISIBLE, 20, 15, 1320, 790, 0, 0, hinst, 0)
+	mainWnd, _, _ = pCreateWindowExW.Call(0, uintptr(unsafe.Pointer(cls)), uintptr(unsafe.Pointer(ws("ARMAZEM GRATIDÃO PRO v1.2.17"))), WS_OVERLAPPEDWINDOW|WS_VISIBLE, 20, 15, 1320, 790, 0, 0, hinst, 0)
